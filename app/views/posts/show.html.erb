<% provide(:title, "Bloggies || #{@post.title}") %>

<small>
  Author: <%= @post.user.name %> (<%= @post.created_at %>)
</small>
<p>
  <%= @post.content %>
</p>

<%= link_to "My posts", posts_path %>
